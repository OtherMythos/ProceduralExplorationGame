_tIntegration("CharacterInspectorToolTest", "Sanity test to check that the character inspector tool can be started up", {
    "steps": [
        function(){
            ::_testHelper.waitFrames(50);
        }
    ]
});